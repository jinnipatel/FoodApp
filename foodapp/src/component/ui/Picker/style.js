import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    picker: {
        height: 40,
        width: 200,
        color: 'grey'
      },
      container: {
        height: 50,
        width: 250,
        paddingTop: 0,
        alignItems: "center"
      },
})

export default styles