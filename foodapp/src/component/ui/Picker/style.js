import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    picker: {
        height: 40,
        width: 200,
        marginLeft:45,
        color: 'grey'
      },
      container: {
        height: 40,
        width: 280,
        paddingTop: 0,
        alignItems: "center"
      },
})

export default styles